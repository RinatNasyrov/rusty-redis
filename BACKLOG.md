# Сделать дальше
- Обработка Null массивов и больших строк - при любой ошибке считывания размера считаем его Null
- Отдельный метод для форматирования строк ответов в RESP из Input
- Параметры подключения вынести в конфиг файл
- Кароче пока будем принимать ввод команд исключительно капсом потому, что я ебал отвелаться хочу MVP, потом подумаю чо сделать
- Оригинальный редис агрится если передавать части команды как симпл стринг и ждет балк стринг, нужно подогнать поведение прогр под это
- Добавить в Input возможноть хранить nil для $ и * (с длиной -1 и без содержания) потому, что GET например возвращает nil балк стринг, если ничего не находит
- Возвращать из каждой ветки execute_command(..) RESPMessenge, а не вручную собранные в строку ответы
- Хранить в словарях "value_struct = (value_itself, save_timestamp, ttl)" чтобы реализовать логику с ттл (пока думаю при возвращении результата чекать, что данные не устарели, если да то выбрасывать, чтоб не мониторить нон стопом весь хешмап)
- Выводить в лог пришедшие команды
